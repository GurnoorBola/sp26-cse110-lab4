1. The bug was that num1 and num2 were strings so adding them together was concatenating them together instead of adding the numeric values.
2. I would fix it by converting it from a string to a number so they can be properly added. 
